[package]
name = "SystemWeaver"
version = "0.1.0"
edition = "2024"

[dependencies]
egui = "0.33.2"
eframe = { version = "0.33.2", default-features = false, features = [
    "default_fonts",
    "glow",
] }
